{
  "name": "cronocapilar-functions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/vertexai": "^1.9.2",
    "firebase-admin": "12.7.0",
    "firebase-functions": "^6.2.0",
    "stripe": "^17.5.0",
    "twilio": "^5.3.7",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vitest": "^4.0.18"
  }
}
