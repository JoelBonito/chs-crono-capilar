# LOG DIÁRIO — 2026-02-12
- Projeto: CronoCapilar
- Fuso: America/Sao_Paulo

## Sessões
1. 08:20 — 08:35 (00:15)
   - Atividades:
     - Diagnóstico de erro na execução de `npm run dev` na raiz do projeto.
     - Atualização do `package.json` raiz para incluir scripts de proxy (`dev`, `build`, `start`, `lint`) que redirecionam para o diretório `web`.
     - Correção de erro de build do Tailwind CSS v4 (`Cannot apply...`) no `globals.css`:
       - Adição de mapeamento de variáveis CSS (`--color-primary`, `--color-border`, etc.) para o bloco `@theme` para gerar as classes utilitárias necessárias.

## Resumo do Dia
- Início do dia: 08:20
- Fim do dia: 08:35
- Tempo total: 00:15
